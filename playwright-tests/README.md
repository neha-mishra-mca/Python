# Playwright Test Suite - Finding Therapists on Map (Germany)

## Setup
1. python -m pip install -r requirements.txt
2. playwright install
3. adjust BASE_URL in conftest.py to point to your test environment

## Run tests
pytest -q

## Notes
- Update selectors to match your application HTML attributes.
